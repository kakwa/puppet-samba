source 'https://rubygems.org'

puppetversion = ENV.key?('PUPPET_VERSION') ? "= #{ENV['PUPPET_VERSION']}" : ['>= 5']
gem 'puppet', puppetversion
gem 'puppetlabs_spec_helper', '>= 2'
gem 'puppet-lint', '>= 2'
gem 'facter', '>= 2'
